name: Generate SDE Database

on:
  workflow_dispatch:
    inputs:
      ru_descriptions:
        description: 'Fetch Russian descriptions from ESI (adds ~30 min)'
        required: false
        default: 'true'
        type: boolean
      build_icons:
        description: 'Generate type icons from EVE game cache'
        required: false
        default: 'true'
        type: boolean
  schedule:
    - cron: '0 6 * * 1'

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install system dependencies
        run: sudo apt-get install -y libyaml-dev

      - name: Install dependencies
        run: pip install pyyaml requests Pillow numpy

      - name: Get week number for cache key
        id: date
        run: echo "week=$(date +%Y-%W)" >> $GITHUB_OUTPUT

      - name: Cache SDE download
        uses: actions/cache@v4
        id: sde-cache
        with:
          path: /tmp/sde.zip
          key: sde-${{ steps.date.outputs.week }}

      - name: Download SDE
        if: steps.sde-cache.outputs.cache-hit != 'true'
        run: |
          curl -L -o /tmp/sde.zip \
            https://developers.eveonline.com/static-data/eve-online-static-data-latest-yaml.zip

      - name: Generate SQLite database
        run: |
          RU_FLAG=""
          if [ "${{ inputs.ru_descriptions }}" = "true" ]; then
            RU_FLAG="--ru-descriptions"
          fi
          python3 generate_sde.py \
            --sde-zip /tmp/sde.zip \
            --out item_db_en.sqlite \
            $RU_FLAG \
            --workers 20

      - name: Get build number from DB
        id: sde_version
        run: |
          BUILD=$(python3 -c "import sqlite3, time; c=sqlite3.connect('item_db_en.sqlite'); row=c.execute('SELECT build_number FROM version_info LIMIT 1').fetchone(); print(row[0] if row and row[0] else int(time.time()))")
          echo "build=$BUILD" >> $GITHUB_OUTPUT
          echo "SDE build: $BUILD"

      - name: Cache EVE game files
        if: inputs.build_icons != 'false'
        uses: actions/cache@v4
        with:
          path: icon_cache
          key: eve-cache-${{ steps.date.outputs.week }}
          restore-keys: |
            eve-cache-

      - name: Cache icon working directory
        if: inputs.build_icons != 'false'
        uses: actions/cache@v4
        with:
          path: icon_work
          key: icon-work-${{ steps.date.outputs.week }}
          restore-keys: |
            icon-work-

      - name: Generate type icons
        if: inputs.build_icons != 'false'
        run: |
          python3 build_icons.py \
            --out icons.zip \
            --cache-dir icon_cache \
            --icon-dir icon_work \
            --skip-skins

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: sde-${{ steps.sde_version.outputs.build }}
          name: SDE ${{ steps.sde_version.outputs.build }}
          body: |
            EVE SDE database build ${{ steps.sde_version.outputs.build }}

            Generated from CCP Static Data Export on ${{ github.run_id }}
          files: |
            item_db_en.zip
            icons.zip
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
